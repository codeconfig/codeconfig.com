name: Pulumi
on:
  push:
    branches:
      - master
jobs:
  up:
    name: Update
    runs-on: self-hosted
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: 16
          run: npm ci
   
      - name: Installing dependencies 📦️
        run: npm install
      
      - uses: elstudio/action-install-azure-cli@v1
          
      - uses: azure/CLI@v1
        with:
          inlineScript: |
            az login --identity
            
      - name: Applying infrastructure 🚀
        uses: pulumi/actions@v4
        with:
          command: up
          stack-name: cb
          cloud-url: azblob://pulumistate
        env:
          AZURE_STORAGE_ACCOUNT: stracndrunnerclarity0101
          AZURE_KEYVAULT_AUTH_VIA_CLI: "true"
          ARM_USE_MSI: true
          ARM_CLIENT_ID: aa42d34f-7866-47e6-83df-a397fa7e8718
          ARM_CLIENT_SECRET: o7Y8Q~E18g3fgqoLpidfNKUQcqgD01PvZalrbajv
          ARM_SUBSCRIPTION_ID: 0d321a7d-ad1c-49cd-848d-aadd32ce1ff1
